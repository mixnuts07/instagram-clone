import { Text, View } from "react-native";
const Post = ({ post }) => {
  return (
    <View style={{ marginBottom: 30 }}>
      <Text style={{ color: "white" }}>Post</Text>
    </View>
  );
};

export default Post;
